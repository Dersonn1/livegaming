# Assets

General reusable assets (VFX models, sound instances, decorative props) that
command handlers or other game scripts pull in via `ServerStorage.Assets`.
Empty by default in this starter project.
