# Items

Tool/item Models keyed by `itemId`, given to players by the `GIVE_ITEM`
command (see `ServerScriptService/Services/ItemService.lua`). The starter
ItemService only tracks quantities as IntValues; wire actual Tool granting
here once you have real item content.
